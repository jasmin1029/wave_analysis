services:
  - type: web
    name: waveform-analysis
    runtime: python
    buildCommand: sudo apt-get install -y fonts-wqy-microhei && pip install -r requirements.txt
    startCommand: bash start.sh
